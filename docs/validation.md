# 实现验证记录

验证日期：2026-09-16。工具：本机 Garmin Connect IQ SDK 9.2.0，设备目标 `instinct3solar45mm`（SDK 统一覆盖 Solar 45mm / 50mm）。

## 发布产物

- 文件：`bin/field-release.prg`，36,220 bytes（约 35.4 KiB）。
- 编译：通过，无警告。
- 编译器报告前台数据：5,815 bytes；前台代码：13,291 bytes；无扩展代码页。
- SHA-256：`58B237EB0731D7598D6989DCCD7B95C6562368E57AC512D63AD12B213D4659FA`。
- 正式版排除了固定数据 fixture、单元测试和性能日志代码。

## Garmin 模拟器测试

6 组全部通过（passed=6, failed=0, errors=0）：

| 测试 | 覆盖内容 |
| --- | --- |
| formattingAndWeather | 空值、单位、数值简写、风向边界、54 种天气编号、昼夜、过期和未来样本 |
| settingsAndDemand | 配置校验、增加/减少槽位、隐藏槽位不触发传感器需求 |
| sensorScheduling | 未选择传感器零读取、单条样本、采样节流、时钟回拨恢复 |
| renderAllLayouts | 实际 Graphics.Dc 字宽、黑白主题 × 4/6/8 槽、所有指标、大值溢出保护 |
| deviceSettingsMenu | 全部 15 个设置入口及其所有选项资源有效 |
| secondsAndRedrawBudget | 无变化不重绘、秒数更新不采集数据、超预算降级、休眠显示策略 |

生成资源检查：指标 ID 连续且稳定、设置默认值存在、全部字形为 1-bit、中文日期字形齐全。针对测试绘图输出逐像素比对 SDK 原始设备遮罩，六种默认布局的所有数字、中文和图标均无物理裁切。

## 性能诊断

最终诊断构建（包含额外日志，使用真实系统 API）运行 124 次 onUpdate 回调：

- 目标运行时总预算：61,344 bytes；观测峰值占用：34,496 bytes（约 33.7 KiB）。
- 稳态样本：占用 34,448 bytes，剩余 26,896 bytes。
- 共 3 次整屏绘制、8 次最新传感器样本读取、1 次天气缓存读取。
- 秒数模式为默认“仅抬腕”，模拟期间处于休眠，因此秒数绘制为 0。
- 最终样本 `System.getTimer()` 得到的 onUpdate 耗时为 0ms：表示低于该环境可观测的计时粒度，**不表示零 CPU 耗时**。不据此推算真机功耗。
- 可选常显秒数的代码路径与测试确认只更新 24×9 像素，不读历史数据、天气或设置；尚未取得真机逐秒功耗曲线。

## 预览来源与限制

`preview/layouts.png` 由模拟器测试实际执行的 Monkey C 绘图命令和发布字体重建；每条文字的宽度与 SDK 实际返回值一致。`preview/device-preview.png` 将该绘图叠加官方 SDK 设备遮罩，供观察小窗与切角适配。这两张是实现预览，不是真机截图。

没有连接真实手表，尚未验证侧载安装、特定固件的设置菜单入口、天气同步延迟、长期运行稳定性或电池续航。模拟器菜单测试验证了构造和所有资源，尚未进行真实按键操作验收。
